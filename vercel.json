{
  "buildCommand": "npm install && cd ../api && npm install && cd ../frontend && npm install && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/:match*", "destination": "/api/index.js" }
  ]
}

